Mercurial indicator in zsh prompt is slow
  - profile which parts of it are the worst
  - maybe move the whole thing to a single Python script to save invocations?
Revise the osx-defaults values (there's a lot in there!)
Figure out if we can somehow save/restore Atom and Sublime Text package lists
  - Atom possibilities:
    * https://github.com/holman/dotfiles/blob/master/bin/atom-package-install
    * https://github.com/holman/dotfiles/blob/master/bin/atom-package-backup
